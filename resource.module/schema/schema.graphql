# import Resource from 'typeDefs.graphql'

type Query {
  resources: [Resource]!
}

type Mutation {
  depositResource(masterResourceKey: ID!, amount: Int!): Resource!
  withdrawResource(masterResourceKey: ID!, amount: Int!): Resource!
}
