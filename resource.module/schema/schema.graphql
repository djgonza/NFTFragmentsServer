# import Resource from 'typeDefs.graphql'

type Query {
  resources: [Resource]!
}

type Mutation {
  depositResource(masterResourceId: ID!, amount: Int!): Resource!
  withdrawResource(masterResourceId: ID!, amount: Int!): Resource!
}
