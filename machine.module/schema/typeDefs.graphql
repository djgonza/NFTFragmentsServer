# import MasterMachine from '../../master.module/machine/type.graphql'
# import MasterMachineRecipe from '../../master.module/machineRecipe/type.graphql'
# import Land from '../../land.module/schema/typeDefs.graphql'

type Machine {
  _id: ID!
  masterMachine: ID!
  masterMachineData: MasterMachine!
  masterRecipe: ID
  masterRecipeData: MasterMachineRecipe
  landData: Land
  running: Boolean!
  endDate: Float!
}
