# import MasterLand from '../../master.module/land/type.graphql'
# import MasterResource from '../../master.module/resource/type.graphql'
# import Machine from '../../machine.module/schema/typeDefs.graphql'

type LandResourceAmount {
  masterResource: ID!
  masterResourceData: MasterResource!
  amount: Int!
}

type Land {
  _id: ID!
  masterLand: ID!
  masterLandData: MasterLand!
  landResources: [LandResourceAmount]!
  currentMachine: Machine
}
