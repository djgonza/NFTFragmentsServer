# import Land from 'typeDefs.graphql'
# import Machine from '../../machine.module/schema/typeDefs.graphql'

type Query {
  lands: [Land]!
}

type Mutation {
  createLand(masterLandKey: ID!): Land!
  placeMachineInLand(_id: ID!, machineID: ID!): Machine!
  removeMachineFromLand(_id: ID!, machineID: ID!): Land!
}
