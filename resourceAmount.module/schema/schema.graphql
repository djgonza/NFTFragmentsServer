# import ResourceAmount from 'typeDefs.graphql'

type Query {
  resourcesAmount: [ResourceAmount]!
}

type Mutation {
  createResourceAmount(element: Float!, masterResource: ID!, amount: Int!): ResourceAmount!
  withdrawResourceAmount(element: Float!, masterResource: Float!, amount: Int!): ResourceAmount!
}
