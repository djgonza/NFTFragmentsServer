# import MasterMachineRecipe from '../machineRecipe/type.graphql'

type MasterMachine {
  key: ID!
  name: String!
  tier: Int!
  masterMachineRecipes: [MasterMachineRecipe]!
}

type Query {
  masterMachines: [MasterMachine]!
}