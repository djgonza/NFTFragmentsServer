# import Part from 'typeDefs.graphql'

type Query {
  parts: [Part]!
}

type Mutation {
  depositPart(masterPartKey: ID!, amount: Int!): Part!
  withdrawPart(masterPartKey: ID!, amount: Int!): Part!
}
